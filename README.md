# Telegram Video Downloader

Railway variables:
- BOT_TOKEN = BotFather token
- ADMIN_ID = your numeric Telegram ID

The bot supports Dari/English, forced channel membership, downloader, SQLite admin panel, channel add/remove, broadcast, block/unblock, and statistics.

Important: the bot must be an administrator of every required Telegram channel so Telegram can verify membership.
